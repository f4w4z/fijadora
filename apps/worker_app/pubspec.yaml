name: worker_app
description: Fijadora - Worker App
publish_to: 'none'
version: 1.0.0+1

resolution: workspace
environment:
  sdk: ^3.12.2

dependencies:
  flutter:
    sdk: flutter
  flutter_localizations:
    sdk: flutter
  fijadora_core:
    path: ../../packages/fijadora_core

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^6.0.0

flutter:
  uses-material-design: true
