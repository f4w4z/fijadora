<?xml version="1.0" encoding="utf-8"?>
<network-security-config>
    <!-- Supabase API endpoint with optional certificate pinning -->
    <domain-config cleartextTrafficPermitted="false">
        <domain includeSubdomains="true">nmcxkoahokihzqnfkmvg.supabase.co</domain>
        <!-- Trust user-installed CAs only for debug builds -->
        <trust-anchors>
            <certificates src="system" />
        </trust-anchors>
    </domain-config>
    <!-- Allow cleartext for local development only -->
    <base-config cleartextTrafficPermitted="false">
        <trust-anchors>
            <certificates src="system" />
        </trust-anchors>
    </base-config>
</network-security-config>
